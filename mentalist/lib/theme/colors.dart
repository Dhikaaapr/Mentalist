import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF4C6FFF); // Biru utama
  static const Color primarySoft = Color(0xFFE8EEFF); // Warna biru lembut
  static const Color background = Color(0xFFF5F7FB); // Background soft white
  static const Color surface = Colors.white;

  static const Color textMain = Color(0xFF1F2933);
  static const Color textSecondary = Color(0xFF6B7280);

  static const Color border = Color(0xFFE5E7EB);

  static const Color danger = Color(0xFFEF4444);
}
